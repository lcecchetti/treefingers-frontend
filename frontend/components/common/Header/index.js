import { Button, Container } from "components/ui";
import Link from "next/link";

const Header = () => {

  return (
    <Container>
      <div>
        LOGO
      </div>
      <div>
        <Button>Login</Button>
      </div>
    </Container>
  );
};

export default Header;